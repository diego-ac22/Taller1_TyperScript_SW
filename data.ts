export class Serie
{
    constructor(public id: number,public nombres: string, public canales: string, public temporadas: number, public descripcion: string, public link:string, public imagen:string)
    {

    }
}